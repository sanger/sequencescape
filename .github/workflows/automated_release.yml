name: Automated build of project and release creation

on:
  push:
    branches:
      - y25-099-3-migrate-actions

env:
  BRANCH_NAME: ${{ github.ref_name }}

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: sanger/.github/.github/actions/setup/checkout@y25-099-3-migrate-actions

      - uses: nelonoel/branch-name@v1.0.1

      - name: Setup Ruby
        uses: sanger/.github/.github/actions/setup/ruby@y25-099-3-migrate-actions

      - name: Setup Node
        uses: sanger/.github/.github/actions/setup/node@y25-099-3-migrate-actions

      - name: Setup Environment
        uses: sanger/.github/.github/actions/setup/env@y25-099-3-migrate-actions
        with:
          dbport: ${{ job.services.mysql.ports[3306] }}

      - name: Get release version
        id: get_version
        run: echo "RELEASE_VERSION=$(cat .release-version)" >> "$GITHUB_ENV"

      # Create a release tag based on the branch name and .release-version file
      - name: Set release tag
        id: set_tag
        uses: sanger/.github/.github/actions/release/set-release-tag@y25-099-3-migrate-actions
        with:
          branch_name: ${{ env.BRANCH_NAME }}
          release_version: ${{ env.RELEASE_VERSION }}
          github_run_id: ${{ github.run_id }}

      # Actually run our build
      - name: Compile Build
        run: ./compile-build '' v${{ env.RELEASE_VERSION }} ${{ env.BRANCH_NAME }}

      # Create a GitHub release with the release asset as an artifact
      - name: Create release and upload release.tar.gz
        uses: sanger/.github/.github/actions/release/create-release@y25-099-3-migrate-actions
        with:
          release_name: ${{ env.RELEASE_VERSION }}
          github_ref: ${{ github.ref }}
          commi_sha: ${{ github.sha }}

