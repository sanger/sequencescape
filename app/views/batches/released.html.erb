

<% add :back_menu, "Back to pipeline" => pipeline_path(@pipeline) -%>

<%= page_title @pipeline.name, "Released Batches" %>

<%= render partial: "shared/batches", locals: { batches: @batches, in_progress: false, showing_all: true, batch_state: 'released' } %>
