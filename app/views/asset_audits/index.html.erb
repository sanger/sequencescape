
<script type="text/javascript">
  (function($){
    $(function(){
      // Set the activity monitors iframe to fill the yield window
      $('#yield').height(800);
      $('#activity-logging').height(800);

    });
  })(jQuery);
</script>

<%- add :about, "Activity Logging" %>
<iframe id="activity-logging" src="<%= configatron.asset_audits_url  %>">
</iframe>
<%= help_link 'Adding processes to asset audits', 'Adding processes to asset audits' %>
