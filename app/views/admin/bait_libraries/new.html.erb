
<%= page_title 'Bait Library', 'New' %>

<%= form_for([:admin,@bait_library]) do |f| %>
  <%= render_error_messages(@bait_library) %>

  <%= render partial: "edit_form", locals: {f: f } %>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_bait_libraries_path %>
