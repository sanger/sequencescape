
<div class="page-header"><h1>New Plate Purpose</h1></div>

<%= form_for([:admin,@plate_purpose]) do |f| %>
  <%= error_messages_for(f.object) %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', admin_plate_purposes_path %>
