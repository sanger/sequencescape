
<%- add :about, "Provides advanced accessioning tools for administrators" %>

<%= page_title 'Accessioning', 'Tools' %>

<div class="card my-3">
  <div class="card-header font-weight-bold">Sequencescape Accessioning Checklist</div>
  <div class="card-body">
    <%= tag.div(class: 'checks p-2') do %>
      <%= action_checklist_item(
            condition: accessioning_enabled?,
            good: "Accessioning is <strong>Enabled</strong>".html_safe,
            bad: "Accessioning is <strong>Disabled</strong>".html_safe,
            action: "<a href='/flipper/features/y25_706_enable_accessioning'>Change global accessioning</a>".html_safe
          ) %>
      <%= action_checklist_item(
            condition: !Flipper.enabled?(:y25_714_skip_accessioning_tag_validation),
            good: "Accessioning tag validation is <strong>Enabled</strong>".html_safe,
            bad: "Accessioning tag validation is <strong>Disabled</strong>".html_safe,
            action: "<a href='/flipper/features/y25_714_skip_accessioning_tag_validation'>Change accessioning tag validation</a>".html_safe
          ) %>
      <%= action_checklist_item(
            condition: Flipper.enabled?(:y26_094_email_users_on_accessioning_failures),
            good: "User emails on sample failures are <strong>Enabled</strong>".html_safe,
            bad: "User emails on sample failures are <strong>Disabled</strong>".html_safe,
            action: "<a href='/flipper/features/y26_094_email_users_on_accessioning_failures'>Change user email notifications</a>".html_safe
          ) %>
      <%= action_checklist_item(
            condition: Flipper.enabled?(:y25_705_notify_on_external_accessioning_validation_failures),
            good: "Developer notifications on external sample failures are <strong>Enabled</strong>".html_safe,
            bad: "Developer notifications on external sample failures are <strong>Disabled</strong>".html_safe,
            action: "<a href='/flipper/features/y25_705_notify_on_external_accessioning_validation_failures'>Change developer exception notifications</a>".html_safe
          ) %>
    <% end %>
  </div>
</div>

<div class="card my-3">
  <div class="card-header font-weight-bold">Bulk accessioning</div>
  <div class="card-header">
    <ul class="nav nav-tabs card-header-tabs" id="accessioningTab" role="tablist">
      <li class="nav-item" role="presentation">
        <button class="nav-link active" id="accession-by-date-tab" data-toggle="tab" data-target="#accession-by-date" type="button" role="tab" aria-controls="accession-by-date" aria-selected="true">By date range</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="accession-by-name-tab" data-toggle="tab" data-target="#accession-by-name" type="button" role="tab" aria-controls="accession-by-name" aria-selected="false">By name</button>
      </li>
      <li class="nav-item" role="presentation">
        <button class="nav-link" id="clear-accessions-tab" data-toggle="tab" data-target="#clear-accessions" type="button" role="tab" aria-controls="clear-accessions" aria-selected="false">Clear numbers</button>
      </li>
    </ul>
  </div>
  <div class="card-body">
    <div class="tab-content" id="accessioningTabContent">
      <div class="tab-pane show active" id="accession-by-date" role="tabpanel" aria-labelledby="accession-by-date-tab">
        <p>Designed for bulk accessioning of samples after an external service outage.</p>
        <%= form_tag bulk_accession_by_date_admin_accessioning_tools_path, method: :put do %>
          <div class="form-row">
            <div class="form-group col-md-6">
              <label for="start_date" class="form-label">Start date</label>
              <%= date_field_tag :start_date, Date.today, class: 'form-control', id: 'start_date' %>
            </div>
            <div class="form-group col-md-6">
              <label for="end_date" class="form-label">End date</label>
              <%= date_field_tag :end_date, Date.today, class: 'form-control', id: 'end_date' %>
            </div>
          </div>
          <div class="mt-2 text-muted">
            <p>Samples to be accessioned: <span id="bulk-accession-preview"></span></p>
            <p>There is no limit to the number of samples that can be accessioned -
            asynchronous progress can be viewed at the <%= link_to 'Delayed Jobs Queue', admin_delayed_jobs_path %>.
            </p>
            <% if Flipper.enabled?(:y26_094_email_users_on_accessioning_failures) %>
            <p>SSRs will receive email notification of any samples that fail to accession.</p>
            <% end %>
          </div>
          <%= submit_tag 'Accession samples in range', class: 'btn btn-primary' %>
        <% end %>
      </div>
      <div class="tab-pane" id="accession-by-name" role="tabpanel" aria-labelledby="accession-by-name-tab">
        <p>Designed for accessioning a list of selected sample names.</p>
        <%= form_tag bulk_accession_by_name_admin_accessioning_tools_path, method: :put do %>
          <div class="form-row">
            <div class="form-group col-md-12">
              <label for="accession_sample_names" class="form-label">Sample names only (one per line)</label>
              <%= text_area_tag 'sample_names', nil, class: 'form-control', id: 'accession_sample_names', rows: 12 %>
            </div>
          </div>
          <%= button_tag 'Accession samples in list', class: 'btn btn-primary' %>
        <% end %>
      </div>
      <div class="tab-pane" id="clear-accessions" role="tabpanel" aria-labelledby="clear-accessions-tab">
        <p>The accession number is only removed from Sequencescape, the previously accessioned data remains on EBI's servers.</p>
        <%= form_tag clear_accessions_by_name_admin_accessioning_tools_path, method: :put do %>
          <div class="form-row">
            <div class="form-group col-md-12">
              <label for="clear_accession_sample_names" class="form-label">Sample names only (one per line)</label>
              <%= text_area_tag 'sample_names', nil, class: 'form-control', id: 'clear_accession_sample_names', rows: 12 %>
            </div>
          </div>
          <%= button_tag 'Clear accession numbers', class: 'btn btn-warning' %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="card my-3">
  <div class="card-header font-weight-bold">View sample accession numbers</div>
  <div class="card-body">
    <p>Designed for confirming the accession numbers of provided samples.</p>
    <form>
      <div class="form-row">
        <div class="form-group col-md-12">
          <label for="view_sample_names" class="form-label">Sample names only (one per line)</label>
          <%= text_area_tag 'sample_names', nil, class: 'form-control', id: 'view_sample_names', rows:12 %>
        </div>
      </div>
      <%= button_tag 'View accession numbers', type: :button, class: 'btn btn-info', id: 'view_sample_accession_numbers_button' %>
    </form>
    <div class="row mt-3">
      <div class="col">
        <table class="table table-sm table-striped" id="sample_accession_numbers_table">
          <thead>
            <tr>
              <th scope="col">Sample name</th>
              <th scope="col">Accession number</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td colspan="2" class="text-center text-muted">Please enter sample names</td>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<%= vite_javascript_tag 'accessioning_tools' %>
