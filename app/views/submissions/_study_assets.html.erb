<%= label_tag(:asset_group, 'Asset Group') %>
<%= collection_select(
  :submission,
  :asset_group,
  @submission_presenter.study.asset_groups, :id, :name,
  :prompt => 'Please select an asset group for this order.'
) %>

