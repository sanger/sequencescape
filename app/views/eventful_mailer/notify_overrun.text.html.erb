Hello,

<p>This is to let you know that Sequencescape has just received billing information for an unrequested lane.</p>

<p><%= @eventful.class %> <%= @eventful.id %>, Run <%= @run %> Lane <%= @lane %>, <%= @message %></p>


<p>
You can view this item's details here:<br />
<a href="<%= url_for(:host => configatron.site_url, :controller => 'items', :action => 'show', :id => @eventful.id) %>"><%= url_for(:host => configatron.site_url, :controller => 'items', :action => 'show', :id => @eventful.id) %></a>
</p>

<p>Many thanks,<br />
<em>Sequencescape</em></p>
