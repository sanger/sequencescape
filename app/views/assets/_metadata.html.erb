
  <div id="metadata-table-container" class='panel panel-default'>
    <div class='panel-heading'>
      <a href='#metadata-table-collapser' role="button" data-toggle='collapse'>
      <h3 class='panel-title'>Metadata
        <small class='show-hide'>Click to toggle</small>
      </h3>
      </a>
    </div>
    <div id="metadata-table-collapser" class="collapse in">
    <% if @asset.custom_metadatum_collection.present?%>
      <table id="metadata-table" class="sortable table striped">
        <colgroup>
          <col class="npg_aliquot_index">
        </colgroup>
        <thead>
          <tr>
            <th><%= link_to "Key", "javascript:void(0);" %></th>
            <th><%= link_to "Value", "javascript:void(0);" %></th>
          </tr>
        </thead>
        <tbody>
          <% @asset.metadata.each do |k, v| %>
            <tr>
              <td><%= k %></td>
              <td><%= v %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <% else %>
        <div class="panel-body">There is no metadata stored for this asset.</div>
      <% end %>
    </div>
  </div>
