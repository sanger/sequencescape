<table width='100%' cellpadding='4' cellspacing='0'>
  <tr class='header'>
    <th><%= link_to_sort 'Name', 'name', @batch.id %></th>
    <th><%= link_to_sort 'Study', 'study', @batch.id %></th>
    <th><%= link_to_sort 'Lanes', 'lanes', @batch.id %></th>
    <th><%= link_to_sort 'Ended', 'ended', @batch.id %></th>
    <th><%= link_to_sort 'Concentration', 'concentration', @batch.id %></th>
    <th><%= link_to_sort 'Start', 'start', @batch.id %></th>
    <th><%= link_to_sort 'End', 'end', @batch.id %></th>
    <th>&nbsp;</th>
    <th>&nbsp;</th>
  </tr>
